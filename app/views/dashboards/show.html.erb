<%= render 'highcharts' %>

<div class="row-fluid">
	<%= render 'header' %>
</div>

<div class="row-fluid">
	<h2>Views <small>Data for the last 30 days.</small></h2>
  	<div id="container" style="min-width: 400px; height: 300px; margin: 0 auto">
    	<%= render 'line_chart' %>
	</div>
</div>
<br>

<%= render 'snapshot' %>

<br>

<div class="row-fluid">
	<%= render 'top_content' %>
	<%= render 'top_traffic_sources' %>
</div>

<div class="row-fluid">
	<%= render 'about_visitors' %>
	<%= render 'search_terms' %>
</div>
