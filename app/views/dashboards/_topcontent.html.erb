<div class="span6">

	<table summary="Your Websites Top Content" class="table"> 
	<h2>Top Content</h2>

	<table class="table">
		<tr>
			<th>Views</th>
			<th>Title</th>
		<tr>

		<% @pages.each do |page| %>

			<tr>
				<td><%= page.pageviews %></td>
				<td><%= truncate(page.page_title, :length => 70 )%></td>
			</tr>
		<% end %>
	</table>

<!-- 		<h2>All profiles</h2>
		<% @profiles.each do |profile| %>
		<%= profile.title %>: <%= profile.id %><br>
		<% end %>

		<h2>All Web Property ID</h2>
		<% @web_properties.each do |property| %>
		<%= property.account_id %>: <%= property.id %><br>
		<% end %> -->
</div>