	<%= render 'highcharts' %>

<div class="row-fluid">
	<%= render 'header' %>
</div>
<div class="row-fluid">
  <div id="container" style="min-width: 400px; height: 400px; margin: 0 auto">
    	<%= render 'line_chart' %>
	</div>
</div>
<br>

<div class="row-fluid">
	<%= render 'snapshot' %>
</div>
<br>

<div class="row-fluid">
	<%= render 'top_traffic_sources' %>
	<%= render 'top_content' %>
</div>
